import React from 'react'
import axios from 'axios'

export default function MovieCard({titles}) {
    return (
        <div>
            <select name="" id="">
                <option value=""></option>

            </select>
        </div>
    )
}
